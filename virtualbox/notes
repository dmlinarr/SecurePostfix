* Virualbox GUI
vms können über bridge adapter miteinander kommunizieren
shared folder: settings > shared folder > + choose folder shareVm and mark auto mount

* Commands:
//mount shared folder
mkdir /media/shared
mount -t vboxsf shareVM /media/shared
//bind
sudo apt install bind9 bind9utils
sudo systemctl restart bind9
sudo systemctl stop systemd-resolved
sudo systemctl disable systemd-resolved
//postfix
sudo apt install postfix
sudo systemctl restart postfix
//conf dns client
sudo apt-get install resolvconf
sudo systemctl enable --now resolvconf.service
//conf interfaces/ip
sudo netplan apply
//send mail
sudo apt install mailutils
echo "Nachrichtentext" | mail -s "Betreff" empfänger@example.com
//set hostname
hostname mail.u10.cyberlab.fhnw.ch

* Config files:
//bind
/etc/bind/named.conf.options
/etc/bind/named.conf.local
/etc/bind/db.u10.cyberlab.fhnw.ch
/run/systemd/resolve/stub-resolv.conf
/etc/resolvconf/resolv.conf.d/head

* Ip list:
mail: 192.168.97.189
ns1: 192.168.97.80
default: 192.168.96.1