* Virualbox GUI
vms können über bridge adapter miteinander kommunizieren
shared folder: settings > shared folder > + choose folder shareVm and mark auto mount

* Commands:
//mount shared folder
mkdir /media/shared
mount -t vboxsf shareVM /media/shared
//bind
sudo apt install bind9 bind9utils
sudo systemctl restart bind9
sudo systemctl stop systemd-resolved
sudo systemctl disable systemd-resolved
//postfix,postconf,s-nail
sudo apt install postfix
sudo systemctl restart postfix
sudo postconf -e 'home_mailbox= Maildir/'
sudo postconf -e 'virtual_alias_maps= hash:/etc/postfix/virtual'
sudo postmap /etc/postfix/virtual
echo 'export MAIL=~/Maildir' | sudo tee -a /etc/bash.bashrc | sudo tee -a /etc/profile.d/mail.sh
source /etc/profile.d/mail.sh
sudo apt install s-nail
sudo nano /etc/s-nail.rc
echo 'init' | s-nail -s 'init' -Snorecord sammy
cat test_message | s-nail -s 'Test email subject line' -r damjan@u10.cyberlab.fhnw.ch reflector@cyberlab.fhnw.ch
//conf interfaces/ip
sudo netplan apply
//spf
sudo apt install postfix-policyd-spf-python

* Ip list:
mail: 192.168.97.189
ns1: 192.168.97.80
default: 192.168.96.1