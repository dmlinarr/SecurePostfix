* vms können über bridge adapter miteinander kommunizieren

* shared folder
1.settings > shared folder > + choose folder shareVm and mark auto mount
2. In vm: mkdir /media/shared dann mount -t vboxsf shareVM /media/shared